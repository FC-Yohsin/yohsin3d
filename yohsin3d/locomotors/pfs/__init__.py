from .pfs_walk import PFSWalk
from .pfs_turn import PFSTurn

'''
def sinusoidal_wave(
        constant_offset,
        amplitude,
        phase_shift,
        phase_sign,
        t, time_period):
    phase_angle = phase_sign * math.pi / 2 + phase_shift
    return constant_offset + amplitude * math.sin(2 * math.pi * t / time_period + phase_angle)
'''
